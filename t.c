

/*
#include
<stdio.h>

#define min(X, X)  ((X) < (Y) ? (X) : (Y))
//#define min(X, Y)  ((X) > (Y) ? (X) : (Y))

//#define min 3

#define ABC 99
#define ABC(n,m) n+m

#define ABC(e,d) e+d

#define XX 2
#define XX 3
*/
#include 
<stdio.h> 
#define ABC(e,e) e+e

extern int ammllm=2;
auto int a = 32;
register char b = 'G';
extern inline int add(int c, int d)
{
static int a;

extern int ghhhkja = 1;
for(int i=0; i<5; i++)
{
   int alslk;
}

continue;

break;

default:

case:

return c+d;
}

struct Point { 
    int x, y; 

}; 
inline int anjn;// \
inline struct LandR sepChannels_8( unsigned char *, unsigned long, unsigned char *, unsigned char *);

inline int main()
{
int x = min(2, 3, 4);   

int y = <int min(2,3);

add(2,3,3);

struct Point p1 = { 0, 1 }; 

+p1;
x->p =1;
p1.p= 20; 
char *a;
 int value = +a;
for( int i=0; i<4; i++)
{
	int x =10;
}



  bool_t  b =1;
   int32_t x;
   int32_t y = 2;

   /* Non-compliant - logically equivalent to x = y + 1 */
   x = y + // comment \
         + z
         + 1;

  

   /* Compliant - intention is made explicit             */
   x = y + // comment
           // + z
         + 1;

  

   /* Non-compliant */
   x = 0; // comment \
   if ( b )
   {
     int j1=0          /* This is always executed     */
   }


int xi = 2; 
   switch (xi) 
   { 
       case 1: printf("Choice is 1"); 
               break; 
       case 2: printf("Choice is 2"); 
                break; 
       case 2: printf("Choice is 3"); 
               break; 
       default: printf("Choice other than 1, 2 and 3"); 
                break;   
default: printf("Choice other than 1, 2 and 3"); 
                break;  
   } 

return 0;
}

void R_3_2 ( void )
{
   bool_t  b =1;
   int32_t x;
   int32_t y = 2;

   /* Non-compliant - logically equivalent to x = y + 1 */
   x = y + // comment \
         + z
         + 1;

  

   /* Compliant - intention is made explicit             */
   x = y + // comment
           // + z
         + 1;

  

   /* Non-compliant */
   x = 0; // comment \
   if ( b )
   {
     int j1=0          /* This is always executed     */
   }

  
}

int main ( void )
{
  R_3_2 ( );

  return 0;
}